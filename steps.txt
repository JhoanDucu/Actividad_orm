Crear instancia en workbench

Crear un virtual environment (pycharm,vscode)

.venv es el nombre del entorno virtual
python -m venv .venv   VSCode
.venv\Scripts\activate    VSCode
 python -m pip install flask  
flask --debug run
python -m pip install flask-sqlalchemy
python -m pip install  marshmallow_sqlalchemy
python -m pip install flask-migrate
pip install PyMySQL (MySql)
pip install Flask-MySQLdb 
pip install psycopg2 (Postgrest)
